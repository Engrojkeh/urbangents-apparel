{
  "name": "urbangents-apparel",
  "version": "1.0.0",
  "description": "Root package.json for Heroku deployment",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "cd backend && npm install"
  }
}
